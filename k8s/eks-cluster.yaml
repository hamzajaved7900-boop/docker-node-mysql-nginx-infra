apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: devops-demo-cluster
  region: us-east-1

fargateProfiles:
  - name: default
    selectors:
      - namespace: default
      - namespace: kube-system